describe("asExternalUri", () => {
	// TODO: We need to mock the implementation of asExternalUri in order to
	// provide mapped URIs when running locally (in tests), without mocking the
	// logic that handles the WS correctly.
});
