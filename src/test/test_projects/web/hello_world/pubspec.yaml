name: hello_world
description: A web app that uses AngularDart Components

environment:
  sdk: '>=2.5.0 <3.0.0'

dependencies:
  angular:
  angular_components:

dev_dependencies:
  angular_test:
  build_runner:
  build_test:
  build_web_compilers:
  test:
