import "package:test/test.dart";

void main() {
  test('pass one', () {
    expect(1, 1);
  });

  test('pass two', () {
    expect(1, 1);
  });

  test('fail one', () {
    expect(1, 1);
  });

  test('fail two', () {
    expect(1, 1);
  });
}
