import "package:test/test.dart";

// This comment is extracted by the test and compared to a text representation
// built from the tree provider in the test. It must be maintained to match
// the results from the tests below.
// == EXPECTED RESULTS ==
// test/short_test.dart [1/1 passed, {duration}ms] (pass.svg)
//     group1 [1/1 passed, {duration}ms] (pass.svg)
//         test1 [{duration}ms] (pass.svg)
// == /EXPECTED RESULTS ==

void main() {
  group("group1", () {
    test("test1", () {});
  });
}
