name: hello_world

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  http:
  convert: ^2.0.1
  my_package:
    path: ../my_package
  protobuf:
    git:
      url: git://github.com/dart-lang/protobuf.git
      path: protobuf

dev_dependencies:
  test: '>=0.12.34'
         # test
 # test
 # test
 # test
 # test
 # test
 # test
 # test
