name: hello_world
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  http:
  convert:
  my_package:
    path: ../my_package
  protobuf:
    git:
      url: git://github.com/dart-lang/protobuf.git
      path: protobuf

dev_dependencies:
  test: '>=0.12.34'
 # test
 # test
 # test
 # test
