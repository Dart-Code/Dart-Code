import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('pass one', (WidgetTester tester) async {
    expect(1, 1);
  });

  testWidgets('pass two', (WidgetTester tester) async {
    expect(1, 1);
  });

  testWidgets('fail one', (WidgetTester tester) async {
    expect(1, 1);
  });

  testWidgets('fail two', (WidgetTester tester) async {
    expect(1, 1);
  });
}
